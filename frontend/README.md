# SWTOR Armory Frontend

React + TypeScript frontend for SWTOR Armory, built with Vite and React Admin.

## Setup

```bash
npm install
npm run dev
```

The frontend reads the API base URL from `VITE_API_URL`. See `.env.example`.

## Checks

```bash
npm run lint
npm run build
```
